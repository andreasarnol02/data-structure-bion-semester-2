name: Java CI/CD Pipeline

# Trigger the workflow on push and pull requests
on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    # Checkout the repository code
    - name: Checkout code
      uses: actions/checkout@v4

    # Set up Java environment
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    # Compile Java files in all directories
    - name: Compile Java files
      run: |
        echo "🔨 Compiling Java files..."
        
        # Find all directories containing Java files
        java_dirs=$(find . -name "*.java" -exec dirname {} \; | sort -u)
        
        if [ -z "$java_dirs" ]; then
          echo "⚠️ No Java files found in the repository"
          exit 1
        fi
        
        # Compile Java files in each directory
        for dir in $java_dirs; do
          echo "📁 Processing directory: $dir"
          if [ -n "$(find "$dir" -maxdepth 1 -name "*.java")" ]; then
            echo "Compiling Java files in: $dir"
            cd "$dir"
            javac *.java
            if [ $? -eq 0 ]; then
              echo "✅ Successfully compiled Java files in: $dir"
            else
              echo "❌ Failed to compile Java files in: $dir"
              exit 1
            fi
            cd - > /dev/null
          fi
        done
    
    # Clean up compiled files
    - name: Cleanup
      run: |
        echo "🧹 Cleaning up compiled files..."
        
        # Find and delete all .class files
        class_files=$(find . -name "*.class")
        if [ -n "$class_files" ]; then
          echo "Removing compiled .class files..."
          find . -name "*.class" -delete
          echo "✅ Cleanup completed!"
        else
          echo "ℹ️ No .class files found to clean up"
        fi